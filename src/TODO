# license


# docs


# term log

git log --pretty=format:"%H" --reverse --after 2012-02-18 |
while read commit
do
  git show --color --word-diff=color --pretty=format:"%n%Cblue%H%Creset %C(yellow)%ci%Creset%n" $commit
done |
most


# timebase

some kind of encoding of timestamps as audio, so cut/paste editing of
sessions can still be linked to code changes (ie, at each cut boundary
get a combined diff of all the changes that were skipped)


# webvtt

would be better than searching on each time change, if only it worked
